@model IEnumerable<During_Travel2.Models.BookingAgency>

@{
    ViewBag.Title = "Index2";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bookingAgencyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bookingAgencyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookingAgencyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookingAgencyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookingAgencyId })
        </td>
    </tr>
}

</table>
